const {GCP_SPEECH_PATH,GCP_PROJECT_ID,BUCKET_NAME} = process.env;
const PATH = "linguini-service";
const SUFFIX = "sav";
const USER_ID = "user_id";

module.exports = {
    GCP_SPEECH_PATH,
    GCP_PROJECT_ID,
    BUCKET_NAME,
    PATH,
    SUFFIX,
    USER_ID,
};